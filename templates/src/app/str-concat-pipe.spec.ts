import { StrConcatPipe } from './str-concat-pipe';

describe('StrConcatPipe', () => {
  it('create an instance', () => {
    const pipe = new StrConcatPipe();
    expect(pipe).toBeTruthy();
  });
});
